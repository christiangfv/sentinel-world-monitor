'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var react$1 = require('@chakra-ui/react');
var NextLink = require('next/link');
var react = require('react');
var interop = require('./interop.cjs');

const LinkEl = interop.interopDefault(NextLink);
const cx = (...classNames) => classNames.filter(Boolean).join(" ");
const Link = react.forwardRef(function Link2(props, ref) {
  const styles = react$1.useStyleConfig("Link", props);
  const { className, isExternal, href, children, ...rest } = react$1.omitThemingProps(props);
  return /* @__PURE__ */ jsxRuntime.jsx(
    react$1.chakra.a,
    {
      target: isExternal ? "_blank" : void 0,
      ref,
      href,
      ...rest,
      className: cx("chakra-link", className),
      __css: styles,
      as: LinkEl,
      children
    }
  );
});

exports.Link = Link;
